<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- vertical divider -->
    <style name="VerticalDivider">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">1dp</item>
        <item name="android:background">@color/holo_blue_light</item>
    </style>

    <!-- Horizontal divider -->
    <style name="HorizontalDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/holo_blue_light</item>
    </style>

    <!-- match_parent -->
    <style name="MatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- wrap_content -->
    <style name="WrapContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- TextView -->
    <style name="TextView" parent="WrapContent">
        <item name="android:textColor">@color/holo_blue_light</item>
        <item name="android:textSize">@dimen/tv_normal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">@null</item>
    </style>

    <style name="TextView.large">
        <item name="android:textSize">@dimen/tv_large</item>
    </style>

    <!-- TextView in linearLayout -->
    <style name="TextViewInLinear" parent="TextView">
        <item name="android:textColor">@color/holo_blue_light</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:paddingLeft">@dimen/padding_small</item>
        <item name="android:paddingRight">@dimen/padding_small</item>
        <item name="android:background">@null</item>
    </style>

    <style name="TextViewInLinear.large">
        <item name="android:textSize">@dimen/tv_large</item>
    </style>

    <!-- TextView in stateBar -->
    <style name="TextView.stateBar">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/tv_small</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginLeft">@dimen/margin_small</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Marquee TextView -->
    <style name="TextViewMarquee">
        <item name="android:layout_width">@dimen/spinnerWidth</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:singleLine">true</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <!-- EditText -->
    <style name="EditText" parent="TextView">
        <item name="android:layout_width">@dimen/et_width</item>
        <item name="android:background">@drawable/roundrect_nobg</item>
        <item name="android:textColor">@color/holo_blue_dark</item>
        <item name="android:textSize">@dimen/tv_small</item>
    </style>

    <style name="EditText.number">
        <item name="android:textColor">@color/red</item>
        <item name="android:inputType">numberDecimal</item>
        <item name="android:text">0</item>
        <item name="android:ems">10</item>
    </style>

    <!-- ImageView -->
    <style name="ImageView" parent="WrapContent">
        <item name="android:scaleType">fitXY</item>
        <item name="android:background">@null</item>
    </style>

    <!-- ListView -->
    <style name="ListView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:divider">@color/holo_blue_light</item>
        <item name="android:dividerHeight">@dimen/gap</item>
        <item name="android:background">@color/black</item>
    </style>

    <!-- Spinner -->
    <style name="Spinner" parent="WrapContent">
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:layout_width">@dimen/spinnerWidth</item>
        <item name="android:textColor">@color/holo_blue_light</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">right|center_vertical</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Btn -->
    <style name="Button" parent="TextView">
        <item name="android:textSize">@dimen/tv_normal</item>
        <item name="android:textColor">@color/holo_blue_light</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">@dimen/padding_small</item>
        <item name="android:paddingRight">@dimen/padding_small</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@drawable/button_selector_color</item>
    </style>

    <!-- Btn.average -->
    <style name="Button.average">
        <item name="android:layout_width">0dp</item>
        <item name="android:paddingTop">@dimen/padding_small</item>
        <item name="android:paddingBottom">@dimen/padding_small</item>
        <item name="android:layout_weight">1</item>
    </style>

    <!-- Btn.average.background -->
    <style name="Button.average.background">
        <item name="android:background">@drawable/button_selector_color</item>
    </style>

    <!-- Linear -->
    <style name="Linear" parent="TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">@dimen/tv_normal</item>
        <item name="android:paddingTop">@dimen/padding_small</item>
        <item name="android:paddingBottom">@dimen/padding_small</item>
        <item name="android:background">@color/transWhite</item>
    </style>

    <style name="LinearWithHeight" parent="Linear">
        <item name="android:layout_height">@dimen/linearHeight</item>
    </style>

    <!-- 主界面 -->
    <style name="TextViewInContent">
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/tv_larger</item>
        <item name="android:paddingLeft">@dimen/padding_small</item>
        <item name="android:paddingRight">@dimen/padding_small</item>
        <item name="android:background">@null</item>
    </style>

</resources>